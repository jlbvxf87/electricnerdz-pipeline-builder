{
  "name": "electricnerdz-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Electric Nerdz action agents — the runtime + agent library.",
  "scripts": {
    "test": "node --test",
    "run:meeting": "node --env-file=.env.local run.js",
    "smtp:check": "node --env-file=.env.local smtp-check.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.14"
  },
  "engines": {
    "node": ">=18"
  }
}
